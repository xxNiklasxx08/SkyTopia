rootProject.name = "SkyTopia"

pluginManagement {
    repositories {
        gradlePluginPortal()
        maven(uri("https://maven.minecraftforge.net/"))
        maven(uri("https://maven.parchmentmc.org/"))
    }
}